---
description: "Start a new Twitter/X thread writing session"
argument-hint: "<instruction> [--refs <dir>] [--examples <a,b>]"
---

This is a shortcut for `/write twitter`. Invoke `/write` with `twitter` as the content type, passing through all other arguments.

Run the `/write` command with the following arguments prepended:

```
twitter $ARGUMENTS
```
