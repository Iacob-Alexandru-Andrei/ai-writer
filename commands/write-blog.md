---
description: "Start a new blog post writing session"
argument-hint: "<instruction> [--refs <dir>] [--examples <a,b>]"
---

This is a shortcut for `/write blog`. Invoke `/write` with `blog` as the content type, passing through all other arguments.

Run the `/write` command with the following arguments prepended:

```
blog $ARGUMENTS
```
