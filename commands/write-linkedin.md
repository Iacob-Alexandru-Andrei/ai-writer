---
description: "Start a new LinkedIn post writing session"
argument-hint: "<instruction> [--refs <dir>] [--examples <a,b>]"
---

This is a shortcut for `/write linkedin`. Invoke `/write` with `linkedin` as the content type, passing through all other arguments.

Run the `/write` command with the following arguments prepended:

```
linkedin $ARGUMENTS
```
